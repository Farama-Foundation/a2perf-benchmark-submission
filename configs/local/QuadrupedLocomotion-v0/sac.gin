# Basic configuration
train_func.task_name = "dog_pace"
train_func.algorithm = "sac"
train_func.seed = 42
train_func.debug = False

# Environment configuration
train_func.env_name = "QuadrupedLocomotion-v0"

# Training hyperparameters
train_func.num_iterations = 2000000
train_func.batch_size = 256
train_func.learning_rate = 3e-4

# Logging and checkpointing
train_func.eval_interval = 2000
train_func.log_interval = 2000
train_func.train_checkpoint_interval = 20000
train_func.policy_checkpoint_interval = 20000

# Distributed training
train_func.num_replicas = 1
train_func.num_collect_jobs = 11  # Kept from original config
train_func.env_batch_size = 512

# Environment-specific parameters for QuadrupedLocomotion-v0
train_func.motion_file_path = "/home/ike2030/workspace/a2perf/repo_new/a2perf/domains/quadruped_locomotion/motion_imitation/data/motions/dog_pace.txt"

# Distributed training configurations
train_func.replay_buffer_server_address = "127.0.0.1"
train_func.variable_container_server_address = "127.0.0.1"
train_func.replay_buffer_server_port = 8008
train_func.variable_container_server_port = 8008

# Minari configurations
train_func.dataset_id = "QuadrupedLocomotion-dog_pace-novice-v0"

# SAC configuration
train_func.max_sequence_length = 100  # Kept from original config
train_func.replay_buffer_capacity = 2000000  # Updated as per SAC hyperparameters
